# SSH 可视化终端管理工具 · 界面框图

以 ASCII 框图呈现全部界面的布局与关键交互，与[界面设计](./03-界面设计.md)逐节对应。尺寸、色彩（`--xxx` 变量）、快捷键均以界面设计文档为准；surface 形态（window / panel / statusbar）见[技术架构 §6](./02-技术架构.md)。

## 1 图例

```text
┌───────────┐   常规区块（bg2 背景）
├───────────┤
│           │   虚线 = 弹层 / 覆盖层 / 折叠区
┊  …        ┊   「…」= 省略内容
◀── 240px ──►   箭头 = 尺寸标注
[遮罩 60%]     弹层遮罩不透明度
◆●○           连接状态灯（异常 / 在线 / 离线，见 §3）
[执行]        按钮；[批准执行][拒绝] = 多按钮并列
--accent--    色值用 CSS 变量名标注
```

## 2 全局布局

对应界面设计 §2.1 / §2.5。

```text
┌────────────────────────────────────────────────────────────────────────────┐
│                              应用窗口（浏览器）                           │
├─────────────────┬──────────────────────────────────────────────────────────┤
│                 │   ┌───────────────────────────────────────────────────┐  │
│                 │   │ Tab 栏 36px                                       │  │
│                 │   │ ┌─────────┐┌─────────┐┌────────────────────────┐ │  │
│                 │   │ │● web-01 ││○ web-02 ││            │            │ │  │
│                 │   │ └─────────┘└─────────┘└────────────────────────┘ │  │
│                 │   │   ▲ 活跃 Tab：底部 2px --accent 下划线             │  │
│                 │   ├───────────────────────────────────────────────────┤  │
│                 │   │                    桌面区（flex-1）               │  │
│    侧栏 240px    │   │                                                   │  │
│  （Cmd/Ctrl+B    │   │                                                   │  │
│   折叠为 56px）  │   │          ┌─────────────────────────────────────┐   │  │
│                 │   │          │ 见 §4 桌面                            │   │  │
│  见 §3 侧栏框图  │   │          │（图标网格 + 状态卡片 panel + 任务栏）   │   │  │
│                 │   │          └─────────────────────────────────────┘   │  │
│                 │   │                                                   │  │
│                 │   ├───────────────────────────────────────────────────┤  │
│                 │   │ 任务栏 40px（桌面 Tab 内部，见 §4）                 │  │
│                 │   └───────────────────────────────────────────────────┘  │
└─────────────────┴──────────────────────────────────────────────────────────┘
```

- 左侧固定侧栏管理连接列表；右侧为 Tab 栏 + 桌面内容区
- **一个连接只对应一个 Tab**：重复打开（侧栏项 / 历史项 / `ssh://` Deep Link）只切换聚焦，不新建
- 每个 Tab：状态灯（6px 圆点）+ 连接名 + 关闭按钮；支持拖拽排序
- 关闭有运行中窗口的 Tab 时弹确认框

## 3 侧栏

对应界面设计 §2.2 / §2.3 / §2.4 / §4.5。

```text
┌──────────────────┐                     ┌──────────────┐
│ ┌──────────────┐ │                     │  ▉▉ Logo 图标 │  ← 折叠态 56px
│ │    Logo      │ │  ← 48px             └──────────────┘
│ └──────────────┘ │                     ┌──────────────┐
│ ┌──────────────┐ │                     │    🔍        │
│ │  🔍 搜索连接  │ │  ← 实时过滤名称/IP/标签
│ └──────────────┘ │                     └──────────────┘
│ ┌──────────────┐ │                     ┌──────────────┐
│ │ [书签] [历史] │ │  ← 顶部 Tab 切换      │ ▉▉▉▉▉▉▉▉▉▉▉▉▉ │  ← 图标列表
│ └──────────────┘ │                     └──────────────┘
│ ▍开发          [2]│  ← 分组：3px 色条 + 名称 + 计数
│  │ ● web-01    │  │                     ┌──────────────┐
│  │ │ 10.0.0.1  │  │  ← 状态灯+名称+IP    │ + 新建连接    │
│  │ ○ web-02    │  │                     ├──────────────┤
│  │  10.0.0.2   │  │                     │ ⚙ 设置        │
│ ▍生产          [1]│                     └──────────────┘
│  │ ✕ db-prod   │  │  ← ✕ = 连接失败
│  │  10.0.0.3   │  │
│ └──────────────┘ │
│ ┌──────────────┐ │
│ │ + 新建连接    │ │
│ ├──────────────┤ │
│ │ ⚙ 设置       │ │
│ └──────────────┘ │
└──────────────────┘
```

| 状态灯 | 含义 | 颜色 |
| --- | --- | --- |
| ● | 在线 | `--accent` 绿色实心 |
| ○ | 离线 | `--muted` 灰色实心 |
| ◐ | 连接中 | `--warn` 黄色脉冲 |
| ✕ | 异常 | `--danger` 红色实心 |

- 连接项 hover 显示 连接 / 编辑 / 删除 图标按钮
- 连接项右键：连接、复制连接、编辑、复制连接信息、移动到分组、删除
- 分组右键：重命名、添加连接、删除分组
- 分组默认色：Production 红 / Development 蓝 / Personal 绿
- 历史页按使用频率排序，最多 50 条

## 4 桌面

对应界面设计 §3.2 / §3.3 / §3.4 / §3.5。

```text
┌────────────────────────────────────────────────────────────────────────────┐
│                          桌面区（--desktop-bg 渐变背景）                     │
│                                                                              │
│  ┌─────────────────────────────────────────────┐  ┌──────────────────┐       │
│  │       桌面图标网格（每列 72px）               │  │  状态卡片 panel   │       │
│  │                                             │  │  （monitor）      │       │
│  │  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐            │  │  宽 180px        │       │
│  │  │ ▉▉▉ │ │ ▉▉▉ │ │ ▉▉▉ │ │ ▉▉▉ │            │  │  ┌──────────┐   │       │
│  │  │ ▉▉▉ │ │ ▉▉▉ │ │ ▉▉▉ │ │ ▉▉▉ │            │  │  │CPU ██░ 42%│   │       │
│  │  └─────┘ └─────┘ └─────┘ └─────┘            │  │  │MEM ██░ 6.8G│   │       │
│  │  Terminal Files Monitor   AI                │  │  │DSK ██░ 60% │   │       │
│  │  (绿色)   (蓝色)   (青色)   (紫色)            │  │  └──────────┘   │       │
│  │                                             │  │ 半透明+模糊      │       │
│  │                                             │  └──────────────────┘       │
│  │          （双击图标打开窗口）                  │    右上角固定槽位             │
│  └─────────────────────────────────────────────┘                            │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │ 任务栏 40px（半透明黑 + 模糊）                                         │ │
│  │ ┌────┐ │  │  ┌───┐┌───┐│   ┌───────────┐   │  ┌───────────────┐      │ │
│  │ │ ⊞  │▏│  │  │▉▉││▉▉││   │ [时钟 10:24]│   │  │ ● web-01 ●    │      │ │
│  │ └────┘ │  │  └───┘└───┘│   │ statusbar  │   │  │ 10.0.0.1       │      │ │
│  │ 启动按钮│  │ 应用快捷区  │   │ 槽位       │   │ 系统托盘          │      │ │
│  │ 32x32  │  │ 底 2px 蓝条│   └───────────┘   │  └───────────────┘      │ │
│  └─────── └──┴───────────┴───────────────────┴──────────────────────────┘ │
└────────────────────────────────────────────────────────────────────────────┘
```

- 桌面图标只对应 `window` surface 应用（terminal / files / monitor / ai / logs）；`panel` / `statusbar` 应用无图标
- 图标单击选中（紫色高亮 + 半透明背景），双击打开窗口
- 桌面空白区右键：刷新、排列图标、新建快捷方式、桌面设置
- 状态卡片为 monitor 的 `panel` surface（自启），每 2s 推送数据
- 任务栏左到右：启动按钮（32x32 ⊞）｜分隔线｜应用快捷区｜statusbar 槽位｜系统托盘
- 应用快捷区图标底 2px 蓝条表示运行中，单击切换聚焦/最小化

## 5 应用窗口

通用窗口骨架（标题栏 32px，内容最小 400x300）：

```text
┌──────────────────────────────────────────┐
│ ══ 标题（连接名 - 应用 #N）        — 口 ✕ │  ← 拖拽标题栏移动；双击最大化
├──────────────────────────────────────────┤
│            应用内容区                     │
│                                          │
│            （见下方各应用）                │
│                                          │
└──────────────────────────────────────────┘
  ▲ 窗口圆角 12px；拖拽边缘调整大小；点击聚焦置顶
```

### 5.1 终端窗口（terminal · window surface）

对应界面设计 §5。默认 640x400，可多开（标题显示 `连接名 - Terminal #N`）。

```text
┌──────────────────────────────────────────────────────────────┐
│ web-01 - Terminal #1                                  — 口 ✕ │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│   $ ls -la                      ← xterm.js 内容区            │
│   drwxr-xr-x  5 root root 4096 Aug 19 10:00 .               │
│   drwxr-xr-x  3 root root 4096 Aug 18 09:00 ..               │
│   $ cd /var/log/                                             │
│   $                                                        ▏ │  ← block 光标
│                                                              │
│                                                              │
│                                                              │
│   ┌────────────────────────────────────────────────────────┐ │
│   │ 搜索栏（Cmd/Ctrl+F 弹出）：[find error____] [Aa] [.*] [>]│ │
│   └────────────────────────────────────────────────────────┘ │
├──────────────────────────────────────────────────────────────┤
│ ● Connected     延迟 200ms           UTF-8 · 80x24          │
└──────────────────────────────────────────────────────────────┘
  ▲ 状态栏 28px：左=状态灯+Connected/Disconnected │ 中=延迟 │ 右=编码·尺寸
```

- 字体 JetBrainsMono 14px，`Cmd/Ctrl+/-` 调字号；`Cmd/Ctrl+K` 清除屏幕
- 搜索栏支持正则 / 大小写敏感 / 全词匹配，匹配高亮 + 上/下一个导航
- 鼠标拖选自动复制；滚轮滚动 10000 行缓冲

#### 拖拽上传浮层（§5.7）

```text
┌──────────────────────────────────────────────────────────────┐
│                         [拖放高亮区]                          │
│                                                              │
│        释放后弹出：                                           │
│        ┌────────────────────────────────────────┐            │
│        │ 上传到远程当前目录        ← 走 SFTP 通道  │            │
│        │ 输入文件路径到终端                       │            │
│        └────────────────────────────────────────┘            │
└──────────────────────────────────────────────────────────────┘
```

#### 断线重连覆盖层（§5.11）

```text
┌──────────────────────────────────────────────────────────────┐
│                          （半透明覆盖层）                      │
│                                                              │
│             ┌──────────────────────────────────┐             │
│             │  Reconnecting... (attempt 2/3)   │             │
│             │     指数退避 2s / 4s / 8s         │             │
│             └──────────────────────────────────┘             │
│        失败后：                                               │
│        ┌──────────────────────────────┐                      │
│        │  连接已断开          [重连]    │                      │
│        └──────────────────────────────┘                      │
└──────────────────────────────────────────────────────────────┘
  ▲ 重连期间保留上次输出内容
```

### 5.2 文件管理窗口（files · window surface）

对应界面设计 §6。默认 720x480。

```text
┌──────────────────────────────────────────────────────────────────────┐
│ web-01 - Files                                                — 口 ✕ │
├──────────────────────────────────────────────────────────────────────┤
│ ‹ ›  /var/log › nginx › 访问日志[✎]   [📁新建][⬆上传][≡视图][⌄排序][↻][⋯]│ ← 工具栏
├──────────────────────────┬───────────────────────────────────────────┤
│ 目录树 240px（可拖拽调宽） │ 文件列表（flex-1）                          │
│  [折叠]                  │  名称        大小      修改时间   类型   权限  │
│ ▾ /                      │  ▸ access.log 1.2M   Aug 19 10:00 log  rw-  │
│   ▾ var                 ▾ │  ▸ error.log  840K   Aug 19 09:59 log  rw-  │
│     ▾ log                │  ▸ nginx.conf 2.4K   Aug 18 09:00 conf rw-  │
│       ▾ nginx            │  ▾ sites/     —      Aug 17 20:00 dir  rwx- │
│          ○ 访问日志       │  [选中：bg3 高亮]                            │
│          ○ error.log     │                                           │
│     ▾ logrotate.d        │                                           │
│  ○ root                  │                                           │
│  ◀ 当前路径高亮 --accent2│                                           │
├──────────────────────────┴───────────────────────────────────────────┤
│ 状态栏 24px：选中 2 项 · 共 128 项 · 可用 48GB      ↑2.3MB/s ↓840KB/s  │
│ 传输队列（折叠态 28px，点击展开）                                       │
│ ▼ ↑ upload.log  45% ██████░░░░░░ 2.3MB/s                    [x]       │
└──────────────────────────────────────────────────────────────────────┘
  ▲ 传输队列展开态 120px：方向箭头 + 文件名 + 进度条 + 速度；完成 3s 后自动移除
  ▲ 视图切换 Cmd/Ctrl+1/2（详细 / 图标）；面包屑逐级可点，点击[✎]进入直接输入路径
```

- 双击文件下载到 Downloads、双击文件夹进入；列头点击排序，列头右键显隐列
- 拖拽：本地→列表上传；列表→窗口外下载；远程→远程弹「移动/复制/取消」；`⌘/Ctrl`=强制复制、`Shift`=强制移动
- 空白处拖拽框选；`Ctrl/Cmd` 点选、`Shift` 范围多选

#### 文件/文件夹右键菜单（App 贡献点聚合）

对应界面设计 §6.6、技术架构 §6.6。内置分组在前，app 贡献项以分隔线隔开。

```text
┌──────────────────────────────┐
│ 下载到…                      │  ← transfer / open 内置
│ 在终端中打开                 │
│──────────────────────────────│
│ 重命名                F2     │  ← manage 内置
│ 删除                 Delete  │
│ 复制路径                     │
│ 查看属性                     │
│──────────────────────────────│
│ ▶ 上传到对象存储             │  ← uploader app 贡献（custom）
│ ▶ 同步该目录到云端           │     （多选时对全部选中项生效）
└──────────────────────────────┘
```

### 5.3 系统监控窗口（monitor · window surface）

对应界面设计 §7。

```text
┌──────────────────────────────────────────────────────────────────────┐
│ web-01 - Monitor                                              — 口 ✕ │
├──────────────────────────────────────────────────────────────────────┤
│ ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐         │
│ │ CPU        │ │ 内存        │ │ 磁盘        │ │ 网络        │  概览卡  │
│ │ 42%        │ │ 6.8 / 16GB │ │ 120/500 GB │ │↑230KB/s    │  4 列    │
│ │ 4 / 8 cores│ │ ██████░░░░ │ │ ███░░░░░░░ │ │↓1.2MB/s    │  12px 内  │
│ │ ∿∿∿∿∿∿∿∿  │ │            │ │            │ │ ∿∿∿∿∿∿∿∿   │  间距    │
│ └────────────┘ └────────────┘ └────────────┘ └────────────┘         │
│                                                                      │
│ ┌────────────────────────────┐  ┌────────────────────────────┐       │
│ │ CPU 使用率（60s 折线图）     │  │ 内存使用（60s 面积图）       │  详细  │
│ │ ∿∿∿∿∿∿∿∿∿∿∿∿∿∿∿∿∿∿∿∿∿∿   │  │ ▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░░░░  │  图表  │
│ │ 0────────────────100%      │  │ used/cache/free 三区        │  2 列  │
│ └────────────────────────────┘  └────────────────────────────┘       │
│ ┌────────────────────────────┐  ┌────────────────────────────┐       │
│ │ 磁盘 IO（60s 双线图）        │  │ 网络流量（60s 双线图）        │  高    │
│ │ read ──  write --          │  │ 上行 ──  下行 --            │  200px │
│ └────────────────────────────┘  └────────────────────────────┘       │
└──────────────────────────────────────────────────────────────────────┘
  ▲ 告警：卡片左边框 + 主数值变色；图表线色同变；点击卡片展开 5 分钟历史
```

- 每 2s 推送数据；告警阈值 CPU>80%/30s、内存/磁盘>85% 黄，CPU>95%/10s、内存/磁盘>95% 红

### 5.4 AI 面板（ai · window surface）

对应界面设计 §8。默认宽 420px，高度自适应桌面区域。

```text
┌──────────────────────────────────────┐
│ 🤖 AI 助手 · web-01           — 口 ✕ │
├──────────────────────────────────────┤
│  （消息列表 flex-1 滚动）             │
│                                      │
│  ┌──────────────────────────────────┐│  ← AI 消息：左对齐，bg2，
│  │ 我来清理 /var/log 的旧日志…       ││    左上圆角 0，其余 12px
│  └──────────────────────────────────┘│
│              ┌────────────────────┐  │  ← 用户消息：右对齐，
│              │ 帮我清理旧日志       │  │    bg 半透明绿
│              └────────────────────┘  │
│  ┌──────────────────────────────────┐│
│  │ ▸ shell  [命令]   [review]       ││  ← 命令卡片
│  │ ┌──────────────────────────────┐ ││     左边框 3px：黄
│  │ │ find /var/log -mtime +30    │ ││     等宽字体代码块
│  │ │   -delete                   │ ││
│  │ └──────────────────────────────┘ ││
│  │      [拒绝]  [批准执行]          │ │
│  └──────────────────────────────────┘│
│  ┌──────────────────────────────────┐│
│  │ ▸ shell  [命令]   [safe]         ││  ← 绿色左边框，单一 [执行]
│  │ ┌──────────────────────────────┐ ││
│  │ │ df -h                        │ ││
│  │ └──────────────────────────────┘ ││
│  │           [执行]                 │ │
│  └──────────────────────────────────┘│
│  ┌──────────────────────────────────┐│
│  │ ▸ shell  [命令]   [block]        ││  ← 红色左边框，无执行按钮
│  │ ┌──────────────────────────────┐ ││
│  │ │ rm -rf /                     │ ││
│  │ └──────────────────────────────┘ ││
│  │   已拦截：rm -rf root             ││
│  └──────────────────────────────────┘│
│                                      │
│  ┌──────────────────────────────────┐│  ← 审计日志展开（§8.7）
│  │ 历史  ▕ 时间 │ 命令 │ 级别 │ 结果│ │
│  │ 10:01  find -delete…  ● review 批准││
│  │ 09:58  df -h…          ● safe   执行││
│  └──────────────────────────────────┘│
├──────────────────────────────────────┤
│  📎  [ 输入消息或命令…（Enter 发送） ]  🕘  ➤│
└──────────────────────────────────────┘
```

- AI 回复 SSE 逐 token 渲染；流式时发送按钮变 [停止生成]
- 命令卡片状态：safe 绿（`--accent`）/ review 黄（`--warn`）/ block 红（`--danger`）3px 左边框
- block 卡显示拦截原因，无执行按钮；review 审批走 `approvalSFn`（见技术架构 §7.3）
- 输入区：附件按钮（📎 截图供 AI 分析）、历史按钮（🕘 审计日志）、发送按钮（绿色背景）

## 6 弹层

### 6.1 新建连接抽屉

对应界面设计 §4.1 / §4.2。右抽屉 420px，遮罩 60%。

```text
┌──────────────────────────────────────────────────────┐  ┌──────────────┐
│                      应用窗口（遮罩 60%）              │  │ 新建连接      │
│                                                      │  ├──────────────┤
│                                                      │  │ 连接名称*     │
│                                                      │  │ [web-04     ]│
│                                                      │  │ 主机地址*     │
│                                                      │  │ [10.0.0.4   ]│
│                                                      │  │ 端口*        │
│                                                      │  │ [22         ]│
│                                                      │  │ 用户名*      │
│                                                      │  │ [root       ]│
│                                                      │  │ 认证方式*    │
│                                                      │  │ [密码 ▾     ]│
│                                                      │  │ 密码         │
│                                                      │  │ [••••••••   ]│
│                                                      │  │ 分组         │
│                                                      │  │ [开发 ▾     ]│
│                                                      │  │ AI 操作开关 [√]│
│                                                      │  │ 生产环境标记 [ ]│
│                                                      │  │             │
│                                                      │  │ [测试连接]   │
│                                                      │  │ ✓ 连接成功 — │
│                                                      │  │  Ubuntu 22.04│
│                                                      │  │  [取消] [保存]│
│                                                      │  └──────────────┘
└──────────────────────────────────────────────────────┘
  ▲ 测试反馈横幅三色：✓ 绿成功 / ✕ 红失败 / ◐ 黄超时（5s）
```

### 6.2 关闭确认弹窗（窗口 / Tab 有运行中进程）

```text
        ┌────────────────────────────────────────┐
        │  ⚠ 终端 #2 有运行中进程                │
        │  关闭将终止该终端会话。                 │
        │      [取消]          [关闭]            │
        └────────────────────────────────────────┘
```

## 7 空状态

对应界面设计 §4.4。首次使用（无任何已保存连接）。

```text
┌─────────────────────┐   ┌──────────────────────────────────────────┐
│ ▍（无分组）          │   │          欢迎使用 SSH 桌面外壳            │
│  ┌───────────────┐  │   │                                            │
│  │┌─────────────┐│  │   │   ┌────────────────────────────────────┐  │
│  ││  +          ││  │   │   │ 添加你的第一个连接                   │  │
│  ││ 添加你的第一  ││  │   │   │ 支持密钥和密码认证                  │  │
│  ││ 个连接       ││  │   │   │         [开始配置]                  │  │
│  ││ 支持密钥和密码││  │   │   └────────────────────────────────────┘  │
│  ││ 认证         ││  │   │                                            │
│  │└─────────────┘│  │   └──────────────────────────────────────────┘
│  └───────────────┘  │    ▲ 虚线边框引导卡片（index 页），点击打开抽屉
│  （虚线边框引导卡片） │
└─────────────────────┘
```

## 8 标注汇总

### 8.1 尺寸

| 区域 | 尺寸 |
| --- | --- |
| 侧栏 | 240px（折叠 56px） |
| Tab 栏 | 36px |
| 任务栏 | 40px |
| 终端状态栏 | 28px |
| 窗口标题栏 | 32px |
| 终端窗口 | 640x400 |
| 文件管理窗口 | 720x480（目录树 240px，状态栏 24px，传输队列 28/120px） |
| AI 面板 | 宽 420px |
| 状态卡片 panel | 宽 180px |
| 桌面图标 | 36x36 + 12px 标签，列宽 72px |
| 新建连接抽屉 | 420px，遮罩 60% |

### 8.2 快捷键

| 快捷键 | 行为 |
| --- | --- |
| `Cmd/Ctrl+B` | 折叠/展开侧栏 |
| `Cmd/Ctrl+K` | 清除终端屏幕 |
| `Cmd/Ctrl+F` | 终端搜索栏 |
| `Cmd/Ctrl+/-` | 终端字号调整 |
| `Enter` / `Shift+Enter` | AI 输入发送 / 换行 |
| `F2` / `Delete` / `F5` | 文件重命名 / 删除 / 刷新 |
| `Ctrl+Shift+N` | 文件管理器新建文件夹 |
| `Ctrl+A` / `Ctrl+C/X/V` | 全选 / 复制·剪切·粘贴（粘贴=上传） |
| `Backspace` / `⌘↑` | 文件管理器返回上级 |
| `Alt+←/→` | 文件管理器后退 / 前进 |
| `Cmd/Ctrl+1/2` | 文件管理器视图切换（详细 / 图标） |

### 8.3 surface 对照

| app | surface | 呈现位置 |
| --- | --- | --- |
| terminal | window（可多开） | 桌面图标 → 终端窗口 |
| files | window | 桌面图标 → 文件管理窗口 |
| monitor | window + panel | 桌面图标 → 监控窗口；右上角状态卡片（自启） |
| clock | statusbar | 任务栏槽位（自启） |
| ai | window | 桌面图标 → AI 面板 |
| logs | window | 桌面图标 → 日志窗口 |
